<%= form_for(@prazo) do |f| %>
  <% if @prazo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prazo.errors.count, "error") %> prohibited this prazo from being saved:</h2>

      <ul>
      <% @prazo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="field">
    <%= f.label :funcao %><br />
    <%= f.text_field :funcao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
