<%= form_for(@evento) do |f| %>
  <% if @evento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@evento.errors.count, "error") %> prohibited this evento from being saved:</h2>

      <ul>
      <% @evento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="field">
    <%= f.label :debido %><br />
    <%= f.check_box :debido %>
  </div>
  
  <div class="field">
    <%= f.label "Grupo Evento" %><br />
    <%= f.collection_select :grupo_evento_id, GrupoEvento.all, :id, :descricao %>
  </div>
</br>
  <div class="actions">
    <%= f.submit 'Criar evento'%>
  </div>
<% end %>
